{
  "name": "@xenarch/cloudflare",
  "version": "0.1.0",
  "description": "Xenarch Tier-2 edge gating — Cloudflare Worker that gates AI bots for static sites with no server-side middleware.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "wrangler": "^4.20.0"
  }
}
